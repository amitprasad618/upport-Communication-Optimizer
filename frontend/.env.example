# Example env for frontend (DO NOT COMMIT SECRETS)
# Set `VITE_API_BASE_URL` to the backend base URL for production builds.
# If left empty, the frontend will call relative paths (useful for local dev with a proxy).

VITE_API_BASE_URL=
